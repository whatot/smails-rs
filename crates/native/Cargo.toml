[package]
name = "smails-native"
version = "0.1.0"
edition = "2024"

[dependencies]
smails-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = { version = "3.3.0", features = ["json"] }
